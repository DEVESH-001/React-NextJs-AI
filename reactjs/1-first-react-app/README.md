# React + Vite

- This is a simple React application created using Vite. Vite is a fast build tool that provides a modern development experience for web projects. [https://vite.dev/guide/]
- To get started with this project, make sure you have Node.js installed on your machine. Then, you can install the dependencies and run the development server using the following commands:

  npm install
  npm run dev
