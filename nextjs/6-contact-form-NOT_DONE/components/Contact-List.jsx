import { getContacts } from '@/actions'
import React from 'react'

const ContactList = async() => {
    const contacts = awiat getContacts()
  return (
    <div>ContactList</div>
  )
}

export default ContactList